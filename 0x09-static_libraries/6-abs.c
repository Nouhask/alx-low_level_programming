#include "main.h"
/**
 * _abs - calculates the absolute value of an int
 * @q: the int to be checked
 * Return: the absolute value of int
 */
int _abs(int q)
{
	if (q >= 0)
	{
		return (q);
	}
	return (-q);
}
